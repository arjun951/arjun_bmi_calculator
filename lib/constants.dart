import 'dart:ui';
import 'package:flutter/material.dart';

const knoColor = Color(0xFF1B1A3F);
const kactiveColor = Color(0xFF383581);
const snackBar = SnackBar(
  content: Text('Yay! A SnackBar!'),
);